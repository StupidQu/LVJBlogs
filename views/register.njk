{% extends "layout/basic.njk" %}

{% block content %}
<h2 class="ui header">注册一个账号</h2>

<div class="ui divider"></div>

{% if error %}
  <div class="ui negative message">
    <div class="header">
      错误
    </div>
    <p>{{ error }}</p>
  </div>
{% endif %}

<form class="ui form" method="post">
  <div class="field">
    <label>账号</label>
    <input name="username" type="text" placeholder="账号">
  </div>
  <div class="field">
    <label>密码</label>
    <input name="password" type="password" placeholder="密码">
  </div>
  <div class="field">
    <label>邮箱</label>
    <input name="email" type="text" placeholder="邮箱">
  </div>
  <div class="ui message">
    <div class="header">
      注意
    </div>
    <p>请不要使用临时邮箱注册，我们会定期删除这些账号，且您的账号可能会被禁用。</p>
  </div>
  <button class="ui button" type="submit">注册</button>
</form>
{% endblock %}
